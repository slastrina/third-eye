{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "Settings window: core:default only (event listen for backend broadcasts; window show/hide is backend-side via custom IPC commands). MEM115: a window absent from every capability is denied the event ACL, so listen() silently fails while custom-command invokes still work.",
  "windows": ["settings"],
  "permissions": ["core:default"]
}
