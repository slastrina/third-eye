{"default":{"identifier":"default","description":"Overlay window capability; the settings window has its own least-privilege capability in settings.json","local":true,"windows":["overlay"],"permissions":["core:default","core:window:allow-show","core:window:allow-hide","core:window:allow-set-focus","core:window:allow-set-ignore-cursor-events","global-shortcut:allow-is-registered"]},"settings":{"identifier":"settings","description":"Settings window: core:default only (event listen for backend broadcasts; window show/hide is backend-side via custom IPC commands). MEM115: a window absent from every capability is denied the event ACL, so listen() silently fails while custom-command invokes still work.","local":true,"windows":["settings"],"permissions":["core:default"]}}